# 30-desktop (placeholder)

GNOME desktop personalization (not built yet).

Planned scope:
- `dconf` settings dump (~181 keys here), reapplied with `dconf load`.
- Enabled GNOME shell extensions: ding, ubuntu-dock, tiling-assistant, GPaste.
- Extension-specific config (e.g. tiling-assistant).
